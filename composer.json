{
    "name": "sephynox/phpblock",
    "type": "library",
    "description": "A lightweight PHP RPC client for blockchain networks.",
    "keywords": ["php", "ethereum", "rpc", "blockchain"],
    "homepage": "https://github.com/sephynox/phpblock",
    "license": "MIT",
    "authors": [{
        "name": "Tanveer Wahid",
        "email": "tan@wahid.email",
        "homepage": "http://wahid.tech",
        "role": "Developer"
    }],
    "prefer-stable": true,
    "minimum-stability": "stable",
    "config": {
        "platform": {
            "php": "7.4"
        }
    },
    "require": {
        "php": ">=7.4",
        "ext-json": "*",
        "react/event-loop": "^1.1",
        "react/http": "^1.2",
        "react/socket": "^1.6",
        "react/promise": "^2.8",
        "kornrunner/keccak": "^1.1"
    },
    "suggest": {
        "ext-bcmath": "Needed to support Gwei conversions."
    },
    "autoload": {
        "psr-4": {
            "PHPBlock\\": "src/"
        },
        "files": [
            "src/Helper/HexTools.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "PHPBlock\\Tests\\": "tests/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^9",
        "friendsofphp/php-cs-fixer": "3.0.x-dev",
        "phpbench/phpbench": "^0.17.1",
        "brainmaestro/composer-git-hooks": "^2.8"
    },
    "scripts": {
        "tests": [
            "./vendor/bin/phpunit tests"
        ],
        "bench": [
            "./vendor/bin/phpbench run tests/ --report=default"
        ],
        "check": [
            "@tests",
            "@bench"
        ]
    },
    "extra": {
        "hooks": {
            "pre-commit": [
                "echo committing as $(git config user.name)",
                "./vendor/bin/php-cs-fixer fix src/"
            ],
            "pre-push": [
                "./vendor/bin/php-cs-fixer --dry-run src/",
                "./vendor/bin/phpunit tests",
                "./vendor/bin/phpbench run tests/ --report=default"
            ],
            "post-merge": "composer install"
        }
    }
}
